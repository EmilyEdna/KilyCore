
@{
    ViewData["Title"] = "Dictionary";
    Layout = "~/Views/Shared/BaseView.cshtml";
}
@section script{
    <script>
        //配置
        var options = {
            Init: function () {
                this.InitDom();
                this.InitEvents();
            },
            //元素
            InitDom: function () {
                //恢复高度
                $(window).resize(function (e) {
                    window.setTimeout(function () {
                        options.$table.bootstrapTable("resetView", { height: $(window).height() - 150 });
                    }, 200);
                    e.stopPropagation();
                });
                //设置表单
                this.$table = controller.Table("#tableList", { url: "Function/GetSysDicPage", height: $(window).height() - 150, refreshBtn: "#btn-refresh", formElement: "#Edit" });
                if (SysUserInfo.AccountType != 20 && SysUserInfo.AccountType != 10)
                    $("#add").css({ display: "none" });
            },
            //事件
            InitEvents: function () {
                $("#add").on('click', function () {
                    controller.OpenWindow({
                        title: "新增码表",
                        type: 2,
                        height: "400px",
                        width: "600px",
                        url: "FunManage/System/DictionaryEdit",
                        callBack: function (result) {
                            result.$("#Edit").submit();
                        }
                    })
                });
            },
            //行为
            action: {
                TableFormatter: function (val, row, index) {
                    var action = [];
                    if (!row.IsEnable)
                        action.push('<button class="btn btn-sm btn-primary stop"><i class="fa fa-trash-o"></i>禁用</button>');
                    else
                        action.push('<button class="btn btn-sm btn-primary run"><i class="fa fa-trash-o"></i>启用</button>');
                    if (SysUserInfo.AccountType != 20 && SysUserInfo.AccountType != 10)
                        action.push('<button class="btn btn-sm btn-primary edit"><i class="fa fa-pencil-square-o"></i>编辑</button>');
                    return action.join("&nbsp;")
                },
                TableEvents: {
                    "click .stop": function (event, val, row, index) {
                        controller.Confirm("确认禁用？", function (bool) {
                            if (bool) {
                                controller.ajax({
                                    url: "Function/EnableDic",
                                    data: { Id: row.Id, Parameter: false },
                                    type: 'post',
                                    success: function (result) {
                                        options.$table.refresh();
                                    }
                                });
                            }
                        });
                    },
                    "click .run": function (event, val, row, index) {
                        controller.Confirm("确认启用？", function (bool) {
                            if (bool) {
                                controller.ajax({
                                    url: "Function/EnableDic",
                                    data: { Id: row.Id, Parameter: true },
                                    type: 'post',
                                    success: function (result) {
                                        options.$table.refresh();
                                    }
                                });
                            }
                        });
                    },
                    "click .edit": function (event, val, row, index) {
                        controller.OpenWindow({
                            title: "编辑码表",
                            type: 2,
                            height: "400px",
                            width: "600px",
                            url: "FunManage/System/DictionaryEdit?Id=" + row.Id,
                            callBack: function (result) {
                                result.$("#Edit").submit();
                            }
                        })
                    }
                }//表单事件结束
            }//行为结束
        };
        //初始化
        $(document).ready(function () {
            options.Init();
        });
    </script>
}
@section css{

}

<div class="container-fluid">
    <div class="col-lg-12">
        <div class="ibox float-e-margins">
            <div class="ibox-title">
                <h5>系统码表</h5>
            </div>
            <div class="ibox-content">
                <div class="row">
                    <form id="Edit">
                        <div class="col-sm-8 m-b-xs">
                            <div class="input-group">
                                <button type="button" class="btn btn-sm btn-white" id="add"><i class="fa fa-plus"></i>新增</button>
                            </div>
                        </div>
                        <div class="col-sm-4">
                            <div class="input-group">
                                <input type="text" placeholder="价目名称" class="input-sm form-control" name="DicName">
                                <span class="input-group-btn">
                                    <button type="submit" class="btn btn-sm btn-primary"><i class="fa fa-search"></i>搜索</button>
                                    <button type="button" id="btn-refresh" class="btn btn-sm btn-primary"><i class="fa fa-refresh"></i>刷新</button>
                                </span>
                            </div>
                        </div>
                    </form>
                </div>
                <div class="table-responsive">
                    <table id="tableList" class="table">
                        <thead>
                            <tr>
                                <th data-field="DicName" data-halign="center" data-align="center" data-valign="middle">字典名称</th>
                                <th data-field="DicValue" data-halign="center" data-align="center" data-valign="middle">字典值</th>
                                <th data-field="States" data-halign="center" data-align="center" data-valign="middle">状态</th>
                                <th data-formatter="options.action.TableFormatter" data-events="options.action.TableEvents" data-halign="center" data-align="center" data-valign="middle">操作</th>
                            </tr>
                        </thead>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
